"""Parse `app.bsky.feed.like` and `app.bsky.feed.repost` commits."""


def parse_like_or_repost(event: dict) -> dict:
    """Extract the like/repost columns."""
    print(event)
    raise NotImplementedError
