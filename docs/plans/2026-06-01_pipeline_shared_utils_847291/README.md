# Pipeline Shared Utils Refactor

Plan source: Cursor plan `pipeline_shared_utils_15a6e8b5`.

Extract duplicated config resolution, preprocessing, feature CLI helpers, and curation orchestration into shared modules under `data_platform/utils/`, stage runners, and thin platform entrypoints.
