{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "watchPatterns": ["backend/**", "pyproject.toml", "uv.lock", "railway/backend.json"]
  },
  "deploy": {
    "startCommand": "uvicorn backend.main:app --host 0.0.0.0 --port $PORT",
    "healthcheckPath": "/health"
  }
}
